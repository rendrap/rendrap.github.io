version: 2
jobs:
  deploy:
    docker:
      - image: circleci/ruby:2.6.5
    steps:
      - add_ssh_keys:
          fingerprints:
            - "9c:f4:8f:83:51:37:c5:a5:6c:61:23:8c:8b:55:88:a5"
      - checkout
      - restore_cache:
          keys:
            - v1-bundle-{{ checksum "Gemfile" }}
      - run: bundle install --path vendor/bundle
      - save_cache:
          key: v1-bundle-{{ checksum "Gemfile" }}
          paths:
            - vendor/bundle
      - run: bundle exec jekyll build
      - run:
          name: copy static files and commit
          command: |
            ls -la
            find . -maxdepth 1 ! -name '_site' ! -name '.git' ! -name '.circleci' -exec rm -rf {} \;
            ls -la
            mv _site/* .
            ls -la
            rm -r _site/
            ls -la
            git config --global user.name "rendrap"
            git config --global user.email "rendrapradana@gmail.com"
            git status
            git add .
            git status
            git commit --allow-empty -m "$(git log master -1 --pretty=%B)"
            git status
            git log -1
            git branch -a
            git push -f origin HEAD:develop
            git branch -a
            git log -1
workflows:
  version: 2
  build_and_deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only: master